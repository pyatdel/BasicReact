import Button from "./Button";
import Panel from "./Panel";


function Form({ children }) {
  return (
    <Panel title="Welcome">
      <Button >다크</Button>
      <Button >다크</Button>
    </Panel>
  );
}

export default Form